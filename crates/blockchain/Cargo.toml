[package]
name = "blockchain"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
core-domain = { workspace = true }
async-trait = "0.1"
thiserror = { workspace = true }

[features]
# Детерминированный MockChain — тест-двойник, не входит в production-сборку.
testing = []

[dev-dependencies]
tokio = { workspace = true }
